[package]
name = "screensaver"
version = "0.1.0"
edition = "2021"

[dependencies]
winit = "0.30"
wgpu = "27"
pollster = "0.4"
bytemuck = { version = "1.14", features = ["derive"] }

[profile.release]
opt-level = 3
lto = true
